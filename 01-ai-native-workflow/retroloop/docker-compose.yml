services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: retroloop
      POSTGRES_USER: retroloop
      POSTGRES_PASSWORD: retroloop
    ports:
      - "5432:5432"
    volumes:
      - retroloop_postgres:/var/lib/postgresql/data

volumes:
  retroloop_postgres:
